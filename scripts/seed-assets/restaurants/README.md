# Restaurant seed images

Place the image files referenced by `scripts/data/restaurants.js` in this directory.

Run `npm run seed:restaurants` after all files are present. The script copies them
to `UPLOAD_DIR/restaurants` and stores `/uploads/restaurants/...` URLs in MySQL.

Use images you own, generated images, or images whose licenses permit this use.
